<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/facesenac</item>
        <item name="colorPrimaryDark">@color/facesenac</item>
        <item name="colorAccent">@color/facesenac</item>
        <item name="android:statusBarColor">@color/facesenac</item>
        <item name="android:navigationBarColor">@color/facesenac</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="cardViewStyle">@style/CardView</item>
    </style>

    <style name="AppThemeTwo" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/facesenac</item>
        <item name="colorPrimaryDark">@color/facesenac</item>
        <item name="colorAccent">@color/facesenac</item>
        <item name="android:statusBarColor">@color/facesenac</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorPrimaryInverse">@color/facesenac</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:textColorSecondaryInverse">@color/facesenac</item>
        <item name="android:windowBackground">@color/facesenac</item>
        <item name="android:colorBackground">@color/facesenac</item>
        <item name="android:navigationBarColor">@color/facesenac</item>
    </style>

</resources>
